#include <bits/stdc++.h>
using namespace std;
using LL = long long; 

int main() {
	int n, k;
	cin >> n >> k; 
	map<int, int> counts; 
	for (int i = 0; i < n; i++) {
		int fav;
		cin >> fav; 
		counts[fav]++; 
	}
	
}
