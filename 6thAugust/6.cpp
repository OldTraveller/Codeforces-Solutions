#include <bits/stdc++.h>
using namespace std;
typedef long long LL;
const double PI = 3.141592654;

int main() {
        ios :: sync_with_stdio(false);
        cin.tie(nullptr);
        cout.tie(nullptr);

        int t;
        cin >> t;
        while (t--) {
                int n;
                cin >> n;
                string str;
                cin >> str;
                /*
                        Best would be to check the gap between the 
                        minimum of the first letter and maximum of 
                        second letter. 
                        if 'a' and 'z' are there. Then we need to 
                        check separately. 
                        
                */
        }
}